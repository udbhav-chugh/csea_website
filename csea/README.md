#Following libraries need to be installed to be able to run this web app
* pip install django
* pip install pillow
* pip install django-crispy-forms
* pip install django-phonenumber-field
* pip install phonenumbers
